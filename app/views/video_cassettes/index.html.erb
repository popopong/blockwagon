<%= render 'shared/carousel' %>

<%# Latest videocassettes %>
<div class="container my-5">
  <h4><strong>Latest videocassettes added 😁</strong></h4>
</div>

<div class="d-flex" style="width:100%; overflow:auto">
  <%= render 'shared/vhs_scroll' %>
</div>

<%# Popular section %>
<div class="container my-5">
  <h4><strong>Most popular videocassette rentals ⭐</strong></h4>
</div>

<%# Do a .where(rental_requests are the highest) and assign it to a variable %>

<div class="d-flex" style="width:100%; overflow:auto">
  <%= render 'shared/vhs_scroll' %>
</div>

<%# Action section %>
<div class="container my-5">
  <h4><strong>Want some action? 🏃</strong></h4>
</div>

<%# Do a .where(genre: action) and assign it to a variable %>

<div class="d-flex" style="width:100%; overflow:auto">
  <%= render 'shared/vhs_scroll' %>
</div>

<%# Comedy section %>
<div class="container my-5">
  <h4><strong>Looking for a good laugh? 😂</strong></h4>
</div>

<%# Do a .where(genre: comedy) and assign it to a variable %>

<div class="d-flex" style="width:100%; overflow:auto">
  <%= render 'shared/vhs_scroll' %>
</div>

<%# Romance section %>
<div class="container my-5">
  <h4><strong>Love is in the air ❤️</strong></h4>
</div>

<%# Do a .where(genre: romance) and assign it to a variable %>

<div class="d-flex mb-5" style="width:100%; overflow:auto">
  <%= render 'shared/vhs_scroll' %>
</div>