  <div class="container my-5">
    <h4><strong>Incoming Request 😁</strong></h4>
  </div>
  
  <!-- Only activate scroll bar if more than 5 cards -->
  <% if @incoming_rental_requests.size > 5 %>
    <div class="d-flex incoming_requests" style="width:80%; overflow:auto; margin: auto">
  <% else %>
      <div class="d-flex incoming_requests" style="width:80%; margin: auto">
  <% end %>

    <% @incoming_rental_requests.each do |request| %>
      <%= link_to video_cassette_path(request.video_cassette) do %>
        <div class="card-small mx-2">
          <%= image_tag request.video_cassette.cover_photo, class: "cover-img" %>

          <div class="card-small-info" style= "display: block;" >
            <h5><strong><%= request.video_cassette.title %></strong></h5>
            <p><%= request.video_cassette.year %></p> <p><%= request.video_cassette.price %>.00$</p>
            <%= link_to accept_request_path(request), method: 'patch', data: {confirm: "Accept this request?"} do %>
              <i class="fas fa-check-circle tick"></i>
            <% end %>
            <%= link_to reject_request_path(request), method: 'patch', data: {confirm: "Reject this request?"} do %>
              <i class="fas fa-times-circle cross"></i>
            <% end %>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>

  <div class="container my-5">
    <h4><strong>Outgoing Request 😁</strong></h4>
  </div>

  <% if @outgoing_rental_requests.size > 5 %>
    <div class="d-flex incoming_requests" style="width:80%; overflow:auto; margin: auto">
  <% else %>
      <div class="d-flex incoming_requests" style="width:80%; margin: auto">
  <% end %>
  
    <% @outgoing_rental_requests.each do |request| %>
      <%= link_to video_cassette_path(request.video_cassette) do %>
        <div class="card-small mx-2">
          
          <%= image_tag request.video_cassette.cover_photo, class: "cover-img" %>

          <div class="card-small-info" style="display: inline-block" >
            <h5><strong><%= request.video_cassette.title %></strong></h5>
            <p><%= request.video_cassette.year %></p> <p><%= request.video_cassette.price %>.00$</p>
            <img src="<%= request.video_cassette.user.avatar %>" alt="User profile pic">
          </div>
        </div>
      <% end %>
    <% end %>
  </div>

